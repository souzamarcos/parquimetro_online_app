import colors from './colors';

export const text = {
    fontSize: 16,
    textAlign: 'center',
    color: colors.textColor,
};

export const textTitle = {
    fontSize: 25,
    fontWeight: 'bold',
    textAlign: 'center',
    color: colors.textColor,
};