import cores from './cores';

export const text = {
    fontSize: 16,
    textAlign: 'center',
    color: cores.textColor,
};

export const textTitle = {
    fontSize: 25,
    textAlign: 'center',
    color: cores.textColor,
};