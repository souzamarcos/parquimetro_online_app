import cores from './cores';

export const text = {
    fontSize: 16,
    textAlign: 'center',
    color: cores.textColor,
};

export const textTitle = {
    fontSize: 25,
    fontWeight: 'bold',
    textAlign: 'center',
    color: cores.textColor,
};